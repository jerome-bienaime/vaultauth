import { Meta } from '@storybook/blocks'

<Meta title="React components" />
<div>
  ## React/components

### Case Component

`Case` is the smallest component available.
It contains the vault button.

### Row Component

`Row` is a series of `Case` component, put together
in an array.

</div>
